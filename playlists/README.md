# Playlists
Each playlist is stored as a subfolder here.
Folder structure example:

    playlists/
    ├── My Favourites/
    │   └── playlist.json
    ├── Workout Mix/
    │   └── playlist.json
    └── Late Night/
        └── playlist.json

Do not edit `playlist.json` by hand — use the Mewsic UI.
